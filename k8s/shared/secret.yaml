apiVersion: v1
kind: Secret
metadata:
  name: voting-secrets
  namespace: voting
  labels:
    app.kubernetes.io/name: voting-app
    app.kubernetes.io/component: secrets
    app.kubernetes.io/part-of: voting-app
type: Opaque
stringData:
  # IMPORTANT: never commit real secrets in Git.
  # In production, populate this from AWS Secrets Manager or an external secret operator.
  POSTGRES_PASSWORD: "replace-me-with-aws-secrets-manager-value"
  POSTGRES_USER: "postgres"
  REDIS_PASSWORD: ""
